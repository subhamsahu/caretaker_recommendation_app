# Python dependencies for the Vercel serverless function.
# Mirrors server/requirements.txt and adds:
#   - mangum   : ASGI → AWS Lambda / Vercel bridge
#   - asyncpg  : async PostgreSQL driver (needed when DATABASE_URL is postgres://)

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.30.0
pydantic[email]==2.9.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
