# Copy this file to .env for local dev, or paste each key into Vercel's
# Project → Settings → Environment Variables section.

# ── Database ───────────────────────────────────────────────────────────────
# Local SQLite (default – no action needed):
#   DATABASE_URL=sqlite+aiosqlite:///./kidcare.db
#
# Production PostgreSQL (Neon / Supabase / Railway / etc.):
#   Plain postgres:// / postgresql:// URLs are auto-rewritten to use asyncpg.
DATABASE_URL=postgresql://neondb_owner:npg_DQrnLl3Eati7@ep-damp-glade-aixw9qzs-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ── Security ───────────────────────────────────────────────────────────────
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=60
